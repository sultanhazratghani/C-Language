#include<stdio.h>
#include<conio.h>

#define pi 3.142
#define areaCircle(radius) (pi * radius *radius)
#define areaSquar(length, width) (length * length)
#define areaRectanlge(length, width) (length * width)
#define areaTraingle(base, height) (base * height / 2)
#define areaEllipse(radius1, radius2) (pi * radius1 * radius2)
#define areaTrapezoin(height, side1, side2) (height * (side1 + side2) / 2)
